The scripts will fetch statistical data from a running radio or a game server.

The radio services it can currently fetch data from are:

- SHOUTcast servers

Created by hjpotter92

Using Lua socket.tcp and socket.udp modules.
